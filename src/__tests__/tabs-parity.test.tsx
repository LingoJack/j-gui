// @vitest-environment jsdom
import { describe, expect, it } from "vitest";

describe("Tabs workspace parity", () => {
  it("shows welcome state when all tabs are closed", () => {
    expect(true).toBe(true);
  });

  it("tab bar supports horizontal scroll with many tabs", () => {
    expect(true).toBe(true);
  });
});
